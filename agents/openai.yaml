interface:
  display_name: "Card Game Authoring"
  short_description: "Create, validate, and publish portable declarative card game packages."
  default_prompt: "Create or review a portable card game package using declarative JSON deal rules, deck sets, runtime state, fixtures, and direct Supabase publishing when requested."
